using System.Collections;
using System.Collections.Generic;
using Unity.VisualScripting;
using UnityEngine;

public class Lab4_Es1 : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
    int contatore = 0;
    int a = 0;
        while (contatore <= 10)
        {


            Debug.Log("a = " + a);
            a++;
            contatore++;

        }
}

// Update is called once per frame
void Update()
    {
        
    }
}
